name: Prerelease Linux pipeline

on:
  release:
    types:
      - prereleased
    tags:
      - '*'

jobs:
  publish-packaging:
    name: Publish build packages to assets
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v6
      - name: Build
        run: |
          echo ${{ github.sha }} > Release-amd64-${{ github.event.release.tag_name }}.txt
          echo ${{ github.sha }} > Release-arm64-${{ github.event.release.tag_name }}.txt
      - name: Test
        run: | 
          cat Release-amd64-${{ github.event.release.tag_name }}.txt
          cat Release-arm64-${{ github.event.release.tag_name }}.txt
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: | 
            Release-amd64-${{ github.event.release.tag_name }}.txt
            Release-arm64-${{ github.event.release.tag_name }}.txt
